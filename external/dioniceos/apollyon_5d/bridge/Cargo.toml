[package]
name = "bridge"
version = "0.1.0"
edition = "2021"
authors = ["Sebastian Klemm"]
description = "Integration bridge between 5D framework and Metatron-R"

[dependencies]
core_5d = { path = "../core" }
metatron = { path = "../metatron" }
nalgebra = "0.33"

[dev-dependencies]
approx = "0.5"
