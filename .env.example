# Q⊗DASH Backend Configuration Example
#
# Copy this file to .env and fill in your values
# DO NOT commit .env to git (it's already in .gitignore)

# =============================================================================
# IBM Quantum Configuration
# =============================================================================

# IBM Quantum API Token
# Get your token from: https://quantum.ibm.com/account
# IBM_QUANTUM_TOKEN=your_api_token_here

# IBM Backend Name
# Available backends: ibm_kyoto, ibm_osaka, ibm_brisbane, etc.
# Check https://quantum.ibm.com/services/resources for latest backends
IBM_BACKEND_NAME=ibm_kyoto

# IBM Backend Mode
# Options:
#   - disabled (default): No QPU access, backend is disabled
#   - dry-run: Validate circuits without executing (no QPU time consumed)
#   - enabled: Execute on real IBM hardware (CONSUMES QPU TIME)
#
# IMPORTANT: Only set to 'enabled' when explicitly authorized!
IBM_BACKEND_MODE=disabled

# Maximum shots per job (default: 8192)
IBM_MAX_SHOTS=8192

# =============================================================================
# Backend Registry Configuration
# =============================================================================

# Default backend mode for the registry
# Options:
#   - simulation_only (default): Only use simulators
#   - qpu_enabled_auto: Prefer simulators, allow QPUs if available
#   - force_provider:NAME: Only use backends from specific provider
#
# Examples:
#   BACKEND_REGISTRY_MODE=simulation_only
#   BACKEND_REGISTRY_MODE=qpu_enabled_auto
#   BACKEND_REGISTRY_MODE=force_provider:ibm
BACKEND_REGISTRY_MODE=simulation_only

# =============================================================================
# Telemetry API Configuration
# =============================================================================

# Telemetry server host and port
TELEMETRY_HOST=0.0.0.0
TELEMETRY_PORT=8080

# Log level (trace, debug, info, warn, error)
RUST_LOG=info

# =============================================================================
# dioniceOS Configuration
# =============================================================================

# dioniceOS kernel parameters (optional)
# DIONICE_TIME_STEP=0.01
# DIONICE_COUPLING_STRENGTH=0.5

# =============================================================================
# Safety Reminders
# =============================================================================

# ALWAYS keep IBM_BACKEND_MODE=disabled unless you have explicit authorization
# and understand the cost implications of using real quantum hardware.
#
# For testing and development:
# 1. Use IBM_BACKEND_MODE=dry-run to validate circuits without QPU access
# 2. Use BACKEND_REGISTRY_MODE=simulation_only to ensure no accidental QPU usage
# 3. Only set IBM_BACKEND_MODE=enabled when you're ready to run on real hardware
#
# The default configuration is SAFE - no QPU access is possible.
